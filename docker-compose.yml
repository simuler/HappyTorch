services:
  happytorch:
    image: ghcr.io/rivflyyy/happytorch:latest
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODE=web
      - HOST=0.0.0.0
      - HAPPYTORCH_DB_PATH=/app/data/happytorch.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
